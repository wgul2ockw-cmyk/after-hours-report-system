# วิธีติดตั้ง (ครั้งเดียว ~5 นาที, ฟรีทั้งหมด)

ติดตั้งด้วยบัญชี Google ที่เป็นเจ้าของโฟลเดอร์ Drive กลาง
(`https://drive.google.com/drive/folders/1971PNBG2PL4p9m6W66mhhDXvdJj0cIQf`)

## ขั้นตอน

**1. สร้างโปรเจกต์ Apps Script**
เปิด https://script.google.com → **New project** → ตั้งชื่อ เช่น `ระบบรายงานนอกเวลา`

**2. วางโค้ด**
- ไฟล์ `Code.gs` ที่มีอยู่แล้ว: ลบเนื้อหาเดิม แล้ววางเนื้อหาจากไฟล์ **Code.gs** ทั้งหมด
- กด **＋** ข้าง Files → **HTML** → ตั้งชื่อว่า `Index` (ต้องสะกดตรงนี้เป๊ะ) → ลบเนื้อหาเดิม วางเนื้อหาจากไฟล์ **Index.html** ทั้งหมด
- กด 💾 Save

**3. รัน setup ครั้งเดียว**
- ที่แถบบน เลือกฟังก์ชัน `setup` → กด **Run**
- Google จะขอสิทธิ์ → **Review permissions** → เลือกบัญชีของคุณ → *Advanced* → *Go to … (unsafe)* → **Allow**
  (คำเตือน "unsafe" ปกติสำหรับสคริปต์ที่เขียนเอง — สคริปต์นี้ทำงานในบัญชีคุณเท่านั้น)
- รอจนขึ้น `Setup complete` ใน Log
- ผลลัพธ์: ในโฟลเดอร์ Drive กลางจะมี `TEMPLATE…`, `ระบบรายงาน — Registry`, โฟลเดอร์ `Pending / Archive / PDF` ครบอัตโนมัติ

**4. Deploy เป็นเว็บ**
- **Deploy → New deployment → ⚙️ → Web app**
- Execute as: **Me**
- Who has access: **Anyone** (เพื่อให้แพทย์ทุกคนใช้ได้โดยไม่ต้อง login)
- กด **Deploy** → คัดลอก **Web app URL** → นี่คือลิงก์เว็บไซต์ แจกให้ทุกคนได้เลย

**5. ทดสอบ end-to-end**
- เปิดลิงก์ → สร้างรายงาน → เปิดชีต กรอกผู้ป่วย 2–3 ราย
- แท็บ **② ส่งรายงาน** → วางลิงก์ชีต → กด **ส่ง**
- ตรวจว่า PDF อยู่ใน `PDF/ส.ค. 2569/ศูนย์แพทย์หนองแวง/` และชีตต้นทางย้ายไป `Archive/` (แก้ไขไม่ได้แล้ว)
- ถ้าวางลิงก์ชีตที่ไม่ได้เปิดสิทธิ์ ระบบจะแจ้งให้เปิดแชร์ "ทุกคนที่มีลิงก์" แล้วส่งใหม่ (ชีตที่สร้างจากระบบเปิดสิทธิ์ให้อัตโนมัติอยู่แล้ว)

## การดูแลระบบ (ไม่ต้องแตะโค้ด)

- **เพิ่ม/แก้ ศูนย์แพทย์, แพทย์, ช่วงเวลา** → แก้ในชีต `ระบบรายงาน — Registry` แท็บ `Config` (คอลัมน์ centers / doctors / timeslots)
- **ดูประวัติการส่งทั้งหมด** → แท็บ `Registry` (ทุกรายงาน + ลิงก์ PDF)
- **แก้โค้ดภายหลัง** → แก้ใน Apps Script แล้ว **Deploy → Manage deployments → ✏️ → New version** (URL เดิมไม่เปลี่ยน)

## หมายเหตุความปลอดภัย

ชีตที่ค้างส่งเปิดแบบ "anyone with link can edit" และมีข้อมูลผู้ป่วย (ชื่อ/HN/Diag) — อย่าส่งต่อลิงก์ให้บุคคลภายนอก ระบบจะล็อกเป็น view-only ทันทีที่ส่งเป็น PDF แล้ว
